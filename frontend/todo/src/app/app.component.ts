import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'todo';
  runner = 'Usain Bolt';
  greeting = 'My first web page';
  explenation = 'Actually I made a web application with PHP but this one is the first with angular and Spring'
}
